#include<stdio.h>
int main()
{ int i,x=0,y=0;
    char ch[201];
    scanf("%s",ch);
    for(i=0;ch[i]!='\0';i++)
   { if(ch[i]=='L')
    x-=1;
    else if(ch[i]=='R')
    x+=1;
    else if(ch[i]=='U')
 y+=1;
 else 
 y-=1;
}
printf("%d %d",x,y);
    return 0;
}
