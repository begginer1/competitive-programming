#include<stdio.h>
#include<string.h>
int main()
{ int cz=0,i,co=0,d;
char a[50];
scanf("%s",a);
d=strlen(a);
for(i=0;i<d;i++)    
    {
        if(a[i]==90||a[i]==122)
        cz++;
    else
    co++;
    }
    if(co==(cz*2))
    printf("Yes");
    else
    printf("No");
    return 0;
}
