#include<stdio.h>
int main()
{ int size,i,large,k;
scanf("%d%d",&size,&k);
int a[size];
for(i=0;i<size;i++)
scanf("%d",&a[i]);
large=a[0];
for(i=1;i<size;i++)
if(large<a[i])
large=a[i];
if(large<k)
printf("0");
else
printf("%d",large-k);
return 0;
}
